// Seed test projects and milestones on testnet
const { ethers } = require("hardhat");
async function main() {
  console.log("Seeding test data...");
  // TODO: connect to deployed contracts and seed sample projects
}
main().catch(console.error);
